To Run the Program:
*Save the Excel file and the python code in a same folder.
*Open Windows PowerShell and type " python -m pip install tkinter pandas vaderSentiment " 
*If the above snippet doesn't works, use " py -m pip install tkinter pandas vaderSentiment "
*Now run the code in Python or other IDE.